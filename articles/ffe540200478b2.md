---
title: "【備忘録】Ubuntu + GitHub + Rust(axum) で最小Webサーバを立てるまで"
emoji: "🦀"
type: "tech"
topics: ["rust", "axum", "tokio", "git", "ubuntu", "zenn"]
published: false
---

## この記事の目的

- **毎回同じところで詰まらないための備忘録**
- Ubuntu（VM）上で
  - GitHub と連携
  - Rust を入れて
  - axum で **最小のWebサーバを起動**
- 「今日は何をやったか」を**後から見返して即思い出せる**状態にする

---

## 今日の最終成果


- axum + tokio で `/health` エンドポイント作成
- GitHub リポジトリと **secure-sandbox を正しく連携**
- `curl http://localhost:3000/health` が `ok` を返す

---

## 環境

- Ubuntu（VM）
- Rust 1.92.0
- cargo 1.92.0
- GitHub（PAT 認証）

##気になったこと

-ふとホストOS上でサーバーとアドレス打ったらどうなるか試してみた
-結果
 -VM 内で起動した localhostなので接続できなかった
 - →ポートフォワーディングで設定してあげれば多分接続可能

- ちなみに　curl　の意味は Client for URL (httpリクエストを送る)

